const adyenCheckout = require('../../../../cartridge/client/default/js/adyenCheckout');
window.jQuery = window.$ = require('jquery');

describe('rendering components', () => {
    it('', () => {
    });
});

